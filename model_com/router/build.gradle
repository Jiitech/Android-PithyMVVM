apply plugin: 'com.android.library'
apply from: "${GRADLES.baseConfig}"
apply from: "${GRADLES.processor}"

dependencies {
    api  fileTree(dir: 'libs', include: ['*.jar'])

    api  project(":model_lib")
    api  project(":model_com:pipe")

}